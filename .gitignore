# ── Not published yet ─────────────────────────────────────────────────
# The repository is public (for job applications), the planning stays
# local. docs/ gets published at P6 — drop the line below then.
# One file stays private permanently: docs/06-strategie.md, which holds
# licensing and monetization strategy not meant for outside readers.
docs/

# Outside contributions are not being accepted at this time.
CONTRIBUTING.md
# ──────────────────────────────────────────────────────────────────────

# Build artifacts
/bin/
/dist/
pit

# Go
*.test
*.out
coverage.*

# macOS
.DS_Store

# Local configuration
.env
.env.local
