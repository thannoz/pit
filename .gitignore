# ── Kept private ──────────────────────────────────────────────────────
# The planning stays local; the README is the documentation.
docs/

# Outside contributions are not being accepted at this time.
CONTRIBUTING.md
# ──────────────────────────────────────────────────────────────────────

# Build artifacts
/bin/
/dist/
# Throwaway programs used to drive the library by hand before a command
# exists for it. They belong to a session, not to the repository.
/tmpcheck/
# Anchored: a bare "pit" would also match the cmd/pit/ directory.
/pit

# Go
*.test
*.out
coverage.*

# macOS
.DS_Store

# Local configuration
.env
.env.local
