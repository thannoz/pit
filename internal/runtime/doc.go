// Package runtime owns the container lifecycle of a sandbox: bringing
// services up, tearing them down, executing commands and reading logs.
// The first implementation drives Docker Compose.
package runtime
